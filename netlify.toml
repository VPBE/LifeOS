# =========================================
# LIFEOS — Netlify Configuration
# netlify.toml
# =========================================

# No build command needed (plain HTML/CSS/JS)
[build]
  publish = "."

# Redirect all unknown paths to the custom 404 page
[[redirects]]
  from   = "/*"
  to     = "/404.html"
  status = 404
